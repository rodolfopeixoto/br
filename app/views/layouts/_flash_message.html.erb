<% flash.each do |message_type, message| %>
      <div class="container">
        <div class="row">
          <div class="card-content">
              <p><%= "#{message}" %></p>
          </div>
        </div>
      </div>
<% end %>